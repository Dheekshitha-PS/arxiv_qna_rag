steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/arxivqna:$COMMIT_SHA', '-f', 'app/Dockerfile', 'app']

images:
  - 'gcr.io/$PROJECT_ID/arxivqna:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY
